# Release v0.2.2 — draft for GitHub

## Release title

`v0.2.2 — Runnable proofs for every stack`

## Commands

```bash
git push origin main
git tag v0.2.2
git push origin v0.2.2

gh release create v0.2.2 \
  --title "v0.2.2 — Runnable proofs for every stack" \
  --notes-file docs/release/v0.2.2-notes.md
```
