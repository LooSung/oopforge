# OOPforge Proof Result — 2026-08-20, GPT-5.6 Sol High

## Status

- Result: valid
- Invalid reason: none
- Source commit: `c58d0de367cf996f75654458056292d96aaf5900`
- Starter: `examples/calculator-python-hexagonal`
- Cursor Agent version: `2026.08.11-e8db854`
- Model: `gpt-5.6-sol-high`
- Run ID: `20260820T0903-gpt56sol-high`

## Task

Used the fixed task from the [proof protocol](../README.md#fixed-task).

## Conditions

- Control plugin: none; user-level OOPforge skill links were removed for the run and restored afterward
- Treatment delivery: project-local OOPforge skill at the source commit above
- Manual edits during runs: none
- Same model and starter confirmed: yes
- Treatment skill load confirmed by `.craft/` creation: yes
- Control isolation confirmed by absence of `.craft/`: yes

## Control

- Tests: 14 passed, 1 dependency deprecation warning
- Changed files: 8; 333 insertions, 8 deletions
- Change-attributable violations: 2
  - public mutable `voided_at` lets callers bypass the void invariant
  - `Calculation.perform()` worsened from 24 to 26 lines
- Missing coverage: none
- Architecture-driven rework: 1 round, covering 2 local fixes
- Raw artifact location: system temporary directory under run ID `20260820T0903-gpt56sol-high/control`

## OOPforge

- Tests: 14 passed, 1 dependency deprecation warning
- Changed files: 9; 298 insertions, 7 deletions
- Change-attributable violations: 2
  - public mutable `voided_at` lets callers bypass the void invariant
  - `Calculation.perform()` worsened from 24 to 26 lines
- Missing coverage: none
- Architecture-driven rework: 1 round, covering 2 local fixes
- Treatment-required overhead: `.gitignore` added `.craft/`; counted as an extra changed file, not unrelated scope drift
- Raw artifact location: system temporary directory under run ID `20260820T0903-gpt56sol-high/treatment`

## Finding-by-finding comparison

The machine evaluator reported zero violations for both runs. Human review found
the same two change-attributable violations in each run:

1. Both runs extended the starter's public mutable dataclass with invariant-bearing
   `voided_at`. Resetting it to `None` allows a second void.
2. Both runs changed the already-oversized `Calculation.perform()` from 24 to 26
   lines instead of reducing or containing the violation.

The starter already had presentation-to-infrastructure repository wiring. Neither
run introduced a new dependency direction, so it is recorded as pre-existing and
excluded from the change-attributable count.

The evaluator's zero count is a false negative for public mutable invariant state
and worsened method length. Test and coverage checks agreed with human review.

## Interpretation

This pair is neutral on the primary outcome: both runs passed the same 14 tests,
introduced the same two attributable architecture violations, and need one
architecture-driven rework round. The OOPforge run used fewer changed lines but
one additional operational file for continuity. This single pair does not show
that OOPforge reduces violations or rework for other runs, models, stacks, or
tasks.

## Validity notes

- Starter-architecture bias: the starter already demonstrates hexagonal structure
  but also contains public mutable domain state, an oversized factory method, and
  presentation-to-infrastructure composition.
- Model stochasticity: this is one pair from one model invocation per condition.
- Evaluator limitations: it missed both human-confirmed violations and cannot
  distinguish pre-existing, worsened, and newly introduced issues by itself.
- Other: raw artifacts remain in the local system temporary directory and are not
  a durable public artifact.
