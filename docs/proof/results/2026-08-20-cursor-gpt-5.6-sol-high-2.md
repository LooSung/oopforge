# OOPforge Proof Result — 2026-08-20 pair 2, GPT-5.6 Sol High

## Status

- Result: valid
- Invalid reason: none
- Source commit: `ea763339c60858280f3ce804ac9623c9a78c4186` (`v0.9.10` plus one roadmap docs commit)
- Starter: `examples/calculator-python-hexagonal`
- Cursor Agent version: `2026.08.11-e8db854`
- Model: `gpt-5.6-sol-high`
- Run ID: `20260820T0944-gpt56sol-high`

## Task

Used the fixed task from the [proof protocol](../README.md#fixed-task).

## Conditions

- Control plugin: none; user-level OOPforge skill links were removed for the run and restored afterward
- Treatment delivery: project-local OOPforge skill at the source commit above
- Manual edits during runs: none
- Same model and starter confirmed: yes
- Treatment skill load confirmed by `.craft/` creation: yes
- Control isolation confirmed by absence of `.craft/`: yes

## Control

- Tests: 14 passed, 1 dependency deprecation warning
- Changed files: 9; 395 insertions, 7 deletions
- Machine findings: 4
  - `Calculation.perform()` worsened to 26 lines
  - new `void_calculation` router method is 21 lines
  - two application tests are 21 and 22 lines
- Change-attributable violations (human, production code): 3
  - public mutable `voided_at` lets callers bypass the void invariant
  - `Calculation.perform()` worsened from 24 to 26 lines
  - new `void_calculation` handler is 21 lines
- Missing coverage: none
- Architecture-driven rework: 1 round, covering 3 local fixes
- Raw artifact location: system temporary directory under run ID `20260820T0944-gpt56sol-high/control`

## OOPforge

- Tests: 14 passed, 1 dependency deprecation warning
- Changed files: 9; 293 insertions, 7 deletions
- Machine findings: 1
  - `Calculation.perform()` worsened to 26 lines
- Change-attributable violations (human, production code): 2
  - public mutable `voided_at` lets callers bypass the void invariant
  - `Calculation.perform()` worsened from 24 to 26 lines
- Missing coverage: none
- Architecture-driven rework: 1 round, covering 2 local fixes
- Treatment-required overhead: `.gitignore` added `.craft/`; counted as an extra changed file, not unrelated scope drift
- Raw artifact location: system temporary directory under run ID `20260820T0944-gpt56sol-high/treatment`

## Finding-by-finding comparison

The v0.9.10 evaluator now reports worsened method length. It caught
`Calculation.perform()` in both runs. It also flagged the control router
handler and two control tests. Human review excludes tests, matching the
domain-review catalog, and still counts the control's 21-line production
handler.

Both runs still extend the starter's public mutable dataclass with
invariant-bearing `voided_at`. Resetting it to `None` allows a second void.
There is still no machine rule for that leak.

The starter already had presentation-to-infrastructure repository wiring.
Neither run introduced a new dependency direction, so it is recorded as
pre-existing and excluded from the change-attributable count.

Void eligibility lives in `Calculation.void()` in both runs. Both inject a
clock and ship domain, use-case, and API tests.

## Interpretation

This pair is favorable on hard-rule method length and rework count: the
control needs three production fixes, the OOPforge run needs two, and the
machine count is 4 vs 1. The shared public-mutable invariant leak remains.
This second pair still does not show that OOPforge reduces violations or
rework for other runs, models, stacks, or tasks.

## Validity notes

- Starter-architecture bias: the starter already demonstrates hexagonal
  structure but also contains public mutable domain state, an oversized
  factory method, and presentation-to-infrastructure composition.
- Model stochasticity: this is one pair from one model invocation per
  condition. Source commit differs from pair 1 (`c58d0de`); the evaluator
  now includes `METHOD_TOO_LONG`.
- Evaluator limitations: it still misses public mutable invariant state.
  It reports test methods over 20 lines; human architecture counts do not.
- Other: raw artifacts remain in the local system temporary directory and
  are not a durable public artifact.
