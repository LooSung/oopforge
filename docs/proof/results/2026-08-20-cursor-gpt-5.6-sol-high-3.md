# OOPforge Proof Result — 2026-08-20 pair 3, GPT-5.6 Sol High

## Status

- Result: valid
- Invalid reason: none
- Source commit: `ea763339c60858280f3ce804ac9623c9a78c4186` (`v0.9.10` plus one roadmap docs commit)
- Starter: `examples/calculator-python-hexagonal`
- Cursor Agent version: `2026.08.11-e8db854`
- Model: `gpt-5.6-sol-high`
- Run ID: `20260820T0952-gpt56sol-high`

## Task

Used the fixed task from the [proof protocol](../README.md#fixed-task).

## Conditions

- Control plugin: none; user-level OOPforge skill links were removed for the run and restored afterward
- Treatment delivery: project-local OOPforge skill at the source commit above
- Manual edits during runs: none
- Same model and starter confirmed: yes
- Treatment skill load confirmed by `.craft/` creation: yes
- Control isolation confirmed by absence of `.craft/`: yes

## Control

- Tests: 15 passed, 1 dependency deprecation warning
- Changed files: 11; 396 insertions, 9 deletions
- Machine findings: 2
  - `Calculation.perform()` worsened to 26 lines
  - new `void_calculation` router method is 29 lines
- Change-attributable violations (human, production code): 3
  - public mutable `voided_at` lets callers bypass the void invariant
  - `Calculation.perform()` worsened from 24 to 26 lines
  - new `void_calculation` handler is 29 lines
- Missing coverage: none
- Architecture-driven rework: 1 round, covering 3 local fixes
- Raw artifact location: system temporary directory under run ID `20260820T0952-gpt56sol-high/control`

## OOPforge

- Tests: 14 passed, 1 dependency deprecation warning
- Changed files: 9; 318 insertions, 7 deletions
- Machine findings: 1
  - `Calculation.perform()` worsened to 26 lines
- Change-attributable violations (human, production code): 2
  - public mutable `voided_at` lets callers bypass the void invariant
  - `Calculation.perform()` worsened from 24 to 26 lines
- Missing coverage: none
- Architecture-driven rework: 1 round, covering 2 local fixes
- Treatment-required overhead: `.gitignore` added `.craft/`; counted as an extra changed file, not unrelated scope drift
- Raw artifact location: system temporary directory under run ID `20260820T0952-gpt56sol-high/treatment`

## Finding-by-finding comparison

The evaluator caught worsened `Calculation.perform()` in both runs and the
control's oversized void handler. It still missed the public mutable
`voided_at` field that both runs added to the unfrozen dataclass.

Void eligibility lives in `Calculation.void()` in both runs. Both inject a
clock and ship domain, use-case, and API tests. The starter's
presentation-to-infrastructure repository wiring is pre-existing and excluded.

Control added an extra calculate-service test (15 vs 14). That is extra
coverage, not a treatment miss: all protocol coverage checks passed on both
sides.

## Interpretation

This pair is favorable on hard-rule method length and rework count (control
three production fixes, OOPforge two; machine 2 vs 1). The shared
public-mutable invariant leak remains. Together with pairs 1 and 2 this is
still not a universal effectiveness claim.

## Validity notes

- Starter-architecture bias: public mutable domain state, an oversized
  factory method, and presentation-to-infrastructure composition are already
  in the starter.
- Model stochasticity: third pair, same model and starter, same source
  commit as pair 2.
- Evaluator limitations: no rule for public mutable invariant state.
- Other: raw artifacts remain in the local system temporary directory.
